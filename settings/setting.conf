# CUBRID SQL Analyzer console settings.
#
# The console reads this file only when no analyzer options are provided on the
# command line, or when it is explicitly selected with:
#   java -jar analyzer.jar -conf settings/setting.conf
#
# You can also bypass the structured properties below and provide the exact
# command line with:
# arguments=-sx -xd ./sqlmap -tp

# Optional JDBC driver repository directory.
jdbc.repository.dir=/home/cubrocky/workspace/analyzer_project/com.cubrid.SQLAnalyzer/jdbc

# Source options:
source.type=oracle
source.host=192.168.1.6
source.port=1521
source.sid=xe
source.username=cubrid
source.password=cubrid
#
# source.type=xml
# xml.directory=./sqlmap
# xml.charset=UTF-8

# Target options:
target.type=parser
#
# target.type=jdbc
# target.jdbc.url=jdbc:cubrid:localhost:33000:demodb:::
# target.username=dba
# target.password=
